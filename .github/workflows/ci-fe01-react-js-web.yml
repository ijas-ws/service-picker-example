name: CI fe01-react-js-web
on:
  push:
    branches: ["master", "develop", "qa"]
    paths: "fe01-react-js-web/**"
  pull_request:
    paths: "fe01-react-js-web/**"

  # Allows to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.14.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
          cache-dependency-path: ./fe01-react-js-web/package.json

      - name: Install dependencies
        working-directory: ./fe01-react-js-web
        run: yarn

      - name: Lint
        working-directory: ./fe01-react-js-web
        run: yarn lint

      - name: Build
        working-directory: ./fe01-react-js-web
        run: yarn build:dev

      - name: Test
        working-directory: ./fe01-react-js-web
        run: yarn test
