name: CI api01-node-hapi-pg
on:
  push:
    branches: ["master", "develop", "qa"]
    paths: "api01-node-hapi-pg/**"
  pull_request:
    paths: "api01-node-hapi-pg/**"

  # Allows to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.14.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
          cache-dependency-path: ./api01-node-hapi-pg/package.json

      - name: Install dependencies
        working-directory: ./api01-node-hapi-pg
        run: yarn

      - name: Lint
        working-directory: ./api01-node-hapi-pg
        run: yarn lint

      - name: Build
        working-directory: ./api01-node-hapi-pg
        run: yarn build:dev

      - name: Test
        working-directory: ./api01-node-hapi-pg
        run: yarn test
